import React from 'react';
import FormTemplate from '../components/FormTemplate';

const AddPost = (props) => {
    return ( <FormTemplate handleClick={props.handleClick}/> )
}

export default AddPost;
